# 项目约束

- `eli` 必须支持 Windows、Linux 和 macOS。除非需求明确限定平台，否则新增功能和依赖时必须保持对这三个平台的兼容性。
- 平台特定实现必须通过条件编译隔离，并由 `eli-lib` 对外提供统一的跨平台接口。
- 启动盘发现功能必须枚举当前操作系统中已挂载且可读的分区，不得假定分区均使用 Windows 盘符表示。
- 会修改或删除启动盘内容的破坏性命令，在发现多个候选启动盘且用户未显式指定目标时，必须拒绝执行并提示使用全局 `--bootdisk` 参数；只有候选唯一或已显式指定时才能执行。
- 项目内的代码注释必须使用中文；仅用于 Clap 命令行帮助文本的注释可以使用英文。
- `eli-lib/src/command` 中的命令必须按父级命令组织为目录模块，例如 `command/plugin/mod.rs` 和 `command/bootdisk/mod.rs`；每个子命令的实现必须继续拆分到同名文件，例如 `plugin/list.rs`、`plugin/attr.rs` 和 `plugin/delete.rs`。CLI 作为薄转发层，按父级命令使用 `command/plugin.rs`、`command/bootdisk.rs` 等单文件模块即可；顶层入口只负责全局参数、上下文初始化和命令路由。
- 新增功能或改变现有行为时，必须同步更新对应的单元测试和端到端测试用例。跨平台行为必须在 CI 中分别于 Windows、Linux 和 macOS 上验证。
- 端到端测试脚本统一放在 `tests/e2e/` 中，CI 工作流只负责编排和调用，不得内嵌具体测试实现。
- 所有命令在设计和实现时都必须显式考虑同一进程内并发和多进程并发，包括共享状态、写入原子性、锁粒度、执行顺序、部分失败和结果汇总；无共享状态时也应在设计中明确说明，涉及并发的行为必须有对应测试。
- 外部可执行文件和运行环境检查必须统一通过 `eli-lib` 的通用依赖管理模块完成，命令不得各自实现重复的检查逻辑。中台必须集中注册 `SevenZip` 等已知依赖的枚举值及其可执行文件名、测试参数、超时和成功条件，业务调用方只能声明所需的依赖枚举，不得自行拼装检查规则。可执行文件依赖必须从 `PATH` 中解析，同时检查文件存在性并执行中台为该依赖定义的测试命令；检查成功后应返回已解析的绝对路径供调用方使用。
- 通用依赖管理模块必须使用相互独立的枚举分别表示运行环境和依赖程序，并能区分 `WindowsNormal`、`WindowsPE`、`Linux` 和 `MacOS` 运行环境。任何不支持所有平台或只支持特定运行环境的命令，都必须在执行副作用前调用该中台完成环境依赖检查，并返回明确的不支持环境错误。
- `eli plugin load` 是仅面向 Windows Edgeless PE 运行环境的功能；平台特定的脚本执行实现必须通过条件编译隔离，并由 `eli-lib` 提供统一入口。
- `eli plugin load` 必须通过 `PATH` 查找并调用外部 `7z.exe` 解压，不得硬编码 `%ProgramFiles%\7-Zip\7z.exe` 或引入纯 Rust 7z 解压实现；命令必须接受多个文件或目录路径，目录是否递归查找由显式参数控制，并行加载窗口默认为 2 且可通过参数调整。
- `eli plugin load` 的每个插件包都是相互独立的并行任务：某个包失败不得阻止或回滚其他包，解压完成后应立即继续该包的发布和脚本执行，最后统一汇总所有成功与失败结果。仅要求同一插件包的 CMD 脚本早于 WCS 脚本执行，不得在不同插件包之间设置全局 CMD/WCS 阶段屏障。
- `eli plugin load` 在 `eli-lib` 中只保留一个公开的纯粹加载入口，通过最低必要的选项控制输入展开、递归扫描、并行窗口和 `.7zl` 处理策略，不得为启动加载和热加载拆分多个公开入口。
- PE 启动时的 Hook、错误恢复标记、插件属性筛选、LocalBoost 调度和 Explorer 处理属于启动脚本职责，不得下沉到通用的 `eli plugin load` 逻辑中。
- `eli plugin load` 的插件名称直接从源文件名推导，不提供额外的 `--name` 参数；LocalBoost 加载必须独立组织为 `eli plugin localboost load`。
- Rust 重写旧批处理或 PECMD 逻辑时，不得机械保留仅用于脚本间传参的临时文件；删除前必须先确认没有其他消费者，然后改用函数参数、结构体或进程参数传递。已确认 `tarplug.txt` 在旧实现中仅由 `plugin_loader/load.cmd` 和 `plugin_loader/load.wcs` 之间传递插件包路径，Rust 实现不应保留它。
- LocalBoost 的 `repoPart.txt` 是仓库选择的持久配置，且仍被 GUI、EPT 和卸载逻辑读取，因此在这些消费者迁移前必须兼容；`pluginPath.txt` 和 `unit.txt` 仅用于旧脚本间传参，Rust 实现不得创建或依赖它们。
